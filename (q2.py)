 # Q2: Find and Replace Function Implementation

def find_and_replace(lst, find_val, replace_val):
    """
    Task 1
    - Create a function that searches for all occurrences of a value (find_val) in a given list (lst) and replaces them with another value (replace_val).
    - lst must be a list.
    - Return the modified list.
    """
    # Check if lst is actually a list
    if not isinstance(lst, list):
        return None
    
    # Create modified list by replacing values
    modified_list = []
    for item in lst:
        if item == find_val:
            modified_list.append(replace_val)
        else:
            modified_list.append(item)
    
    return modified_list

# Basic test
test_list = [1, 2, 3, 2, 4, 2]
result = find_and_replace(test_list, 2, 'X')
print(f"Original: {test_list}")
print(f"Result: {result}")


Answer: 

Original: [1, 2, 3, 2, 4, 2]
Result: [1, 'X', 3, 'X', 4, 'X']

Explanation:
Step-by-step process:
Position 0: 1 ≠ 2 → Keep 1
Position 1: 2 = 2 → Replace with 'X'
Position 2: 3 ≠ 2 → Keep 3
Position 3: 2 = 2 → Replace with 'X'
Position 4: 4 ≠ 2 → Keep 4
Position 5: 2 = 2 → Replace with 'X'
Result: All three 2s replaced with 'X', other values unchanged.



# Q2 Task 2: Invoke find_and_replace with specified scenarios

def find_and_replace(lst, find_val, replace_val):
    """
    Create a function that searches for all occurrences of a value (find_val) in a given list (lst) and replaces them with another value (replace_val).
    - lst must be a list.
    - Return the modified list.
    """
    # Check if lst is actually a list
    if not isinstance(lst, list):
        return None
    
    # Create modified list by replacing values
    modified_list = []
    for item in lst:
        if item == find_val:
            modified_list.append(replace_val)
        else:
            modified_list.append(item)
    
    return modified_list

# Task 2: Invoke function with specified scenarios
# Scenario 1: [1, 2, 3, 4, 2, 2], 2, 5
scenario1 = find_and_replace([1, 2, 3, 4, 2, 2], 2, 5)
print(f"Scenario 1 Result: {scenario1}")

# Scenario 2: ["apple", "banana", "apple"], "apple", "orange"
scenario2 = find_and_replace(["apple", "banana", "apple"], "apple", "orange")
print(f"Scenario 2 Result: {scenario2}")

Answer: 

Scenario 1 Result: [1, 5, 3, 4, 5, 5]
Scenario 2 Result: ['orange', 'banana', 'orange'

Explanation:

Scenario 1 Breakdown:
Step-by-step process:
Position 0: 1 ≠ 2 → Keep 1
Position 1: 2 = 2 → Replace with 5
Position 2: 3 ≠ 2 → Keep 3
Position 3: 4 ≠ 2 → Keep 4
Position 4: 2 = 2 → Replace with 5
Position 5: 2 = 2 → Replace with 5

Scenario 2 Breakdown:
Step-by-step process:
Position 0: "apple" = "apple" → Replace with "orange"
Position 1: "banana" ≠ "apple" → Keep "banana"
Position 2: "apple" = "apple" → Replace with "orange"
